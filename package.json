{
  "name": "@scayle/storefront-api-sdk",
  "version": "0.1.1",
  "main": "dist/storefront-api-sdk.mjs",
  "typings": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.8.9",
    "prettier": "^2.8.8",
    "typescript": "^5.2.2",
    "vite": "^4.5.0",
    "vite-plugin-dts": "^3.6.2",
    "vitest": "^0.34.6"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/scayle/storefront-api-ts-sdk.git"
  },
  "license": "MIT",
  "scripts": {
    "build": "tsc && vite build",
    "format": "prettier -w \"src/**/*.ts\"",
    "lint": "prettier -l \"src/**/*.ts\" && tsc --noEmit",
    "test": "yarn lint && vitest"
  }
}
