export enum ProductStandardSorting {
  Price = 'price',
  DateAdded = 'new',
  Reduction = 'reduction',
}

export enum CampaignStandardSorting {
  Id = 'id',
  Reduction = 'reduction',
  StartAt = 'start_at',
  EndAt = 'end_at',
}

export enum SortOrder {
  Ascending = 'asc',
  Descending = 'desc',
}
