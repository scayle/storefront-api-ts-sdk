export enum SortOrder {
  Ascending = 'asc',
  Descending = 'desc',
}

export enum ProductSortOption {
  Price = 'price',
  DateAdded = 'new',
  Reduction = 'reduction',
}

export enum CampaignSortOption {
  Id = 'id',
  Reduction = 'reduction',
  StartAt = 'start_at',
  EndAt = 'end_at',
}
